import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chaznex.musicplayer',
  appName: 'Music Player',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
